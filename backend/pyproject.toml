[project]
name = "ma2-backend"
version = "1.0.0"
description = "MA2 — Multi-Agent Action Planner Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
